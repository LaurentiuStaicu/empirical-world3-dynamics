{
  "name": "world3-empirical-web",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "sync-data": "node scripts/sync-data.mjs",
    "check": "node scripts/sync-data.mjs && tsc --noEmit && node scripts/verify-contract.mjs",
    "dev": "node scripts/sync-data.mjs && vite",
    "build": "node scripts/sync-data.mjs && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "7.0.2",
    "vite": "8.3.0"
  }
}
